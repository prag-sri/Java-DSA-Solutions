//Make The String Great
//LeetCode Link:- https://leetcode.com/problems/make-the-string-great/

class Solution {
    public String makeGood(String s) {
        //STEPS FOR SOLUTION:-
        //1)Create a stack and put each char into the stack one by one 
        //2)Keep a check that if upper case char of st.peek() is equal to the current char- if true then pop the element otherwise push the element into the stack
        //3)At the end of the string store the remaining chars into a string and return the reverse of that string as output
        
        int n= s.length();
        Stack<Character> st= new Stack<>();
        for(int i=0; i<n; i++)
        {
            boolean flag= false;
            if(!st.isEmpty() && (st.peek()-s.charAt(i)==32 || st.peek()-s.charAt(i)==-32))
            {
                st.pop();
                flag= true;
            }
            if(!flag)
                st.push(s.charAt(i));
        }
        String ans="";
        while(!st.isEmpty())
        {
            ans=st.pop()+ans;
        }
        return ans;
    }
}
