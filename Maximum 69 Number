//Maximum 69 Number
//LeetCode Link:- https://leetcode.com/problems/maximum-69-number/

class Solution {
    public int maximum69Number (int num) {
        //STEPS FOR SOLUTION:-
        //1)Convert the given int nums into a string str
        //2)Traverse into the string and change the char i.e. if it is 6 then make it 9 and vice versa
        //3)Take a variable max and after converting each string str into int check if it is smaller or greater than max- store the int value in max if it is greater
        
        String str= Integer.toString(num);
        int n= str.length();
        int max= Integer.valueOf(str);
        for(int i=0; i<n; i++)
        {
            char ch= str.charAt(i);
            String newStr= str;
            if(ch=='9')
                newStr=str.substring(0,i)+'6'+str.substring(i+1,n);
            else if(ch=='6')
                newStr=str.substring(0,i)+'9'+str.substring(i+1,n);
            int newNum= Integer.valueOf(newStr);
            max= Math.max(max,newNum);
        }
        return max;
    }
}
