//Evaluate Reverse Polish Notation
//LeetCode Link:- https://leetcode.com/problems/evaluate-reverse-polish-notation/

class Solution {
    public int evalRPN(String[] tokens) {
        //STEPS FOR SOLUTION:-
        //1)If the token[i] is a not an operator i.e it is not either +,-,*,/ then simply push it into the stack
        //2)If the token[i] is an operator then take out the top two elements of stack and perform maths of the two nums for that particular operator
        //3)Push the ans into the stack
        //4)At the end of the tokens array simply return the peek of the stack as ans
        
        int n= tokens.length;
        Stack<Integer> st= new Stack<>();
        
        for(int i=0; i<n; i++)
        {
            String curr= tokens[i];
            if(curr.compareTo("+")==0)
            {
                int sec= st.pop();
                int first= st.pop();
                st.push(first+sec);
            }
            else if(curr.compareTo("-")==0)
            {
                int sec= st.pop();
                int first= st.pop();
                st.push(first-sec);
            }   
            else if(curr.compareTo("*")==0)
            {
                int sec= st.pop();
                int first= st.pop();
                st.push(first*sec);
            }      
            else if(curr.compareTo("/")==0)
            {
                int sec= st.pop();
                int first= st.pop();
                st.push(first/sec);
            }    
            else
            {
                int num= Integer.valueOf(curr);
                st.push(num);
            }
            //System.out.println(st.peek());
        }
        
        return st.pop();
    }
}
