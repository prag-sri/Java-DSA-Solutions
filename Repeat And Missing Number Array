//Repeat And Missing Number Array
//InterviewBit Link:- https://www.interviewbit.com/problems/repeat-and-missing-number-array/

public class Solution {
    // DO NOT MODIFY THE LIST. IT IS READ ONLY
    public ArrayList<Integer> repeatedNumber(final List<Integer> A) {
        //STEPS FOR SOLUTION:-
        //1)Traverse into the arr and for every positive index arr[i]-1, set the element as negative
        //2)If the element is already negative then add arr[i] to the ans list as that is the repeating no.
        //3)After doing the above 2nd step for all elements of arr, now again traverse into the arr from the beginning and find the single positive element, i+1 is the missing no. add it to ans list
        
        int n= A.size();
        int arr[]= new int[n];
        for(int i=0; i<n; i++)
            arr[i]= A.get(i);
        ArrayList<Integer> ans= new ArrayList<>();
        for(int i=0; i<n; i++)
        {
            int val= arr[i]<0?-arr[i]:arr[i];
            int index= val-1;
            if(arr[index]<0)
                ans.add(val);
            else 
                arr[index]= -1*arr[index];
        }
        for(int i=0; i<n; i++)
        {
            if(arr[i]>0)
            {
                ans.add(i+1);
                break;
            }    
        }
        return ans;
    }
}
