//Merge Sorted Array
//LeetCode Link:- https://leetcode.com/problems/merge-sorted-array/

class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        //STEPS FOR SOLUTION:-
        //1)From index m transfer all elements of nums2 array into nums1
        //2)Sort the array
        
        int i=m, j=0;
        while(i<m+n)
        {
            nums1[i]=nums2[j];
            i++;
            j++;
        }
        Arrays.sort(nums1);
    }
}
